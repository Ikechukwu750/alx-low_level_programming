#include <stdio.h>

/**
 * main - prints "with proper grammar, but the outcome is a piece or art,
 * * Return: 0 will be the return value under normal conditions.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
